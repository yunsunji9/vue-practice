export default {
    SET_NEWS(state, news) {
        state.news = news
    },
    SET_ASKS(state, asks) {
        state.asks = asks
    },
    SET_JOBS(state, jobs) {
        state.jobs = jobs
    },
    SET_USER(state, users) {
        state.users = users
    },
    SET_ITEM(state, items) {
        state.items = items 
    },
    SET_LIST(state, items) {
        state.list = items 
    }
}
